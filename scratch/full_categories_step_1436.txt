=== STEP 1436 (replace_file_content) targeting "c:\\Users\\WOLF\\Documents\\Peptide Wellness Project\\src\\app\\page.tsx" ===
TargetContent:
"      {/* 4. PRODUCT CATEGORIES (IvyRx panel grid) */}\n      <span id=\"catalog\" className=\"block -mt-24 pt-24\"></span>\n      <section className=\"py-24 bg-white border-b border-black/5\">\n        <div className=\"max-w-7xl mx-auto px-4 sm:px-6 lg:px-8\">\n          <div className=\"text-center max-w-2xl mx-auto mb-20\">\n            <span className=\"text-[10px] font-mono font-bold uppercase tracking-widest text-[#0066CC] block mb-2\">Sourcing Catalog</span>\n            <h2 className=\"font-sans text-3xl sm:text-4xl font-bold text-[#1D1D1F] uppercase tracking-wider\">\n              Compounds that target metabolic and cellular pathways.\n            </h2>\n            <p className=\"text-xs text-[#1D1D1F]/60 mt-3 font-mono\">\n              Peptides are short-chain amino acid messengers that coordinate cellular signaling, hormone balance, and tissue repair. Valera provides human-grade compounded peptides sourced from licensed U.S. compounding pharmacies under affiliate physician oversight.\n            </p>\n          </div>\n\n          <div className=\"grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6\">\n            {CATEGORIES.map((cat) => {\n              // Select an icon based on slug\n              let Icon = Activity;\n              if (cat.slug === \"weight-loss\") Icon = Flame;\n              if (cat.slug === \"longevity\") Icon = Sparkles;\n              if (cat.slug === \"recovery\") Icon = ShieldCheck;\n              if (cat.slug === \"hormonal\") Icon = Award;\n              if (cat.slug === \"cognitive\") Icon = Brain;\n              if (cat.slug === \"sexual-wellness\") Icon = Heart;\n              if (cat.slug === \"beauty-skin\") Icon = Droplet;\n\n              return (\n                <Link\n                  key={cat.id}\n                  href={`/categories/${cat.slug}`}\n                  className=\"group relative p-6 bg-[#FAFAFC] border border-black/5 rounded-lg hover-lift transition-all overflow-hidden flex flex-col justify-between min-h-[220px] shadow-sm hover:shadow-md\"\
<truncated 1819 bytes>
ReplacementContent:
"      {/* 4. PRODUCT CATEGORIES (IvyRx panel grid) */}\n      <span id=\"catalog\" className=\"block -mt-24 pt-24\"></span>\n      <section className=\"py-24 bg-[#0A0A0C] border-b border-white/5 relative overflow-hidden\">\n        {/* Background Loop Video */}\n        <div className=\"absolute inset-0 w-full h-full overflow-hidden opacity-10 pointer-events-none\">\n          <video \n            autoPlay \n            loop \n            muted \n            playsInline\n            className=\"w-full h-full object-cover grayscale\"\n            src=\"/videos/collection-pedestals.mp4\"\n          />\n          <div className=\"absolute inset-0 bg-[#0A0A0C]/80\" />\n        </div>\n\n        <div className=\"relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 z-10\">\n          <div className=\"text-center max-w-2xl mx-auto mb-20\">\n            <span className=\"text-[10px] font-mono font-bold uppercase tracking-widest text-[#C8D8E8] block mb-2\">Sourcing Catalog</span>\n            <h2 className=\"font-sans text-3xl sm:text-4xl font-bold text-[#F0EEE9] uppercase tracking-wider\">\n              Compounds that target metabolic and cellular pathways.\n            </h2>\n            <p className=\"text-xs text-[#F0EEE9]/60 mt-3 font-mono\">\n              Peptides are short-chain amino acid messengers that coordinate cellular signaling, hormone balance, and tissue repair. Valera provides human-grade compounded peptides sourced from licensed U.S. compounding pharmacies under affiliate physician oversight.\n            </p>\n          </div>\n\n          <div className=\"grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6\">\n            {CATEGORIES.map((cat) => {\n              // Select an icon based on slug\n              let Icon = Activity;\n              if (cat.slug === \"weight-loss\") Icon = Flame;\n              if (cat.slug === \"longevity\") Icon = Sparkles;\n              if (cat.slug === \"recovery\") Icon = ShieldCheck;\n              if (cat.slug === \"hormonal\") Icon = Award;\n              if (cat.sl
<truncated 2350 bytes>
